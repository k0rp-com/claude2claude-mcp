---
description: Remove a paired peer (by name, fingerprint, or id)
argument-hint: <name|fingerprint|id>
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/unpair.sh:*)
---

!`${CLAUDE_PLUGIN_ROOT}/scripts/unpair.sh "$ARGUMENTS"`
